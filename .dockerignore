# The image builds from ./Dockerfile alone and COPYies nothing from the build
# context -- the source tree is bind-mounted at run time (see
# docker-compose.yml). So exclude everything from the context that is sent to
# the daemon, keeping `docker compose build` fast.
*
